{% macro header(postimage, title) %}
  <header>
    <div>
      {% if postimage %}
        <img src="{{postimage}}" alt="{{title}}" class="logo">
      {% else %}
        <img src="/images/base/no-image.jpg" alt="hack-it-up" class="logo">
      {% endif %}
    </div>
  </header>
{% endmacro %}
